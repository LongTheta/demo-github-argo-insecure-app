# GitHub Actions workflow mirroring GitLab CI pipeline
# Stages: Build → Test → Deploy

name: CI
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag (lowercase for GHCR)
        id: meta
        run: echo "tag=ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]'):latest" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tag }}

  test:
    name: Test
    runs-on: ubuntu-latest
    needs: build
    container:
      image: alpine:latest
      options: --entrypoint ""
    steps:
      - name: Run placeholder tests
        run: |
          echo "Running placeholder tests"
          wget -qO- https://example.com || true

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image tag (lowercase for GHCR)
        id: meta
        run: echo "tag=ghcr.io/$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]'):latest" >> $GITHUB_OUTPUT

      - name: Replace placeholder image in deployment
        run: |
          sed -i "s|image: .*|image: ${{ steps.meta.outputs.tag }}|" k8s/deployment.yaml

      - name: Validate Kubernetes manifests (kubeconform, no cluster required)
        run: |
          docker run --rm -v ${{ github.workspace }}:/work -w /work ghcr.io/yannh/kubeconform:v0.7.0 -summary -output text k8s/
